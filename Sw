// 반품검수기 PWA 설치 조건 충족용 최소 서비스워커 (오프라인 캐싱은 안 하고, 그냥 통과시킴)
self.addEventListener('install', (e) => {
  self.skipWaiting();
});
self.addEventListener('activate', (e) => {
  self.clients.claim();
});
self.addEventListener('fetch', (e) => {
  // 캐싱 없이 그대로 네트워크로 통과 - 오프라인 지원 목적이 아니라 "설치 가능" 조건 충족용
});
